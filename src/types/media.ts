// Media processing type definitions

// Highlights
export interface HighlightResult {
  timestamp: string;
  duration: string;
  clipPath: string;
  description: string;
  reason: string;
  score: number;
}
